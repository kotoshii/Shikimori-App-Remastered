<?xml version="1.0" encoding="utf-8"?>
<!--
    A row rather than a bare CheckBox with text: a CompoundButton offers no control over the gap
    between its box and its label, and the row needs a delete button on the end anyway. The checkbox
    is not clickable itself - the whole row toggles it, which is a bigger target.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:minHeight="56dp"
    android:orientation="horizontal"
    android:paddingStart="@dimen/margin_biggest"
    android:paddingLeft="@dimen/margin_biggest"
    android:paddingEnd="@dimen/margin_normal"
    android:paddingRight="@dimen/margin_normal">

    <CheckBox
        android:id="@+id/hostingCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:focusable="false" />

    <TextView
        android:id="@+id/hostingName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_normal"
        android:layout_marginLeft="@dimen/margin_normal"
        android:layout_weight="1"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    <ImageView
        android:id="@+id/deleteView"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/settings_hosting_filter_delete"
        android:padding="12dp"
        android:src="@drawable/ic_delete_outline"
        android:tint="?attr/colorOnSurface" />

</LinearLayout>
